services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: pxlot
      POSTGRES_PASSWORD: pxlot_dev
      POSTGRES_DB: pxlot
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
